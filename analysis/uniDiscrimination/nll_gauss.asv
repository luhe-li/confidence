function nLL = nll_gauss(lapse, mu, sigma, D)
    
    nLL      = NaN; 
    
    pc  = normcdf(D.comparison_loc, D.standard_loc + mu, sigma)*(1-lapse)+lapse/2;
    nLL = nLL - sum(log(pc.^D.n_r_response.*(1-pc).^(1-D{i}(2,:))));
    


 end   
    
    